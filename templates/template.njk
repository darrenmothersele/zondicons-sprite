<!doctype html>
<html lang="en">
<head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <title>Optimised Zondicons</title>
    <link rel="stylesheet" href="styles.min.css">
</head>
<body class="antialiased text-gray-900 bg-gray-100">
<div class="container mx-auto">
    <div class="text-center">
        <h3 class="text-2xl mt-6 font-light">Zondicons (Optimised)</h3>
        <p class="mt-4">
            The original Zondicons by <a href="https://dribbble.com/steveschoger">Steve Schoger</a>
            processed using <a href="https://www.npmjs.com/package/svgo">SVGO</a>.
        </p>
        <p class="mt-4">
            Click on the button to copy SVG code to clipboard.
        </p>
    </div>
    <div class="mt-12">
        <ul class="flex flex-wrap justify-center">
            {% for item in icons %}
                <li class="m-1">
                    <button type="button" class="flex items-center w-64 bg-white px-4 py-2" onclick="copySvgCode('{{ item.id }}-input')">
                        <span class="mr-4">{{ item.data | safe }}</span>
                        <span class="flex-1 text-left text-sm text-gray-700">{{ item.id }}</span>
                        <span class="text-xs text-gray-600">{{  item.length }}b</span>
                    </button>
                    <textarea class="text-xs w-64 h-4 overflow-hidden bg-gray-200 text-gray-300" type="text" id="{{ item.id }}-input">{{ item.data }}</textarea>
                </li>
            {% endfor %}
        </ul>
        <ul class="flex flex-wrap">
            {% for item in icons %}
                <li class="m-1">
                </li>
            {% endfor %}
        </ul>
    </div>
</div>
<script>
    function copySvgCode(id) {
      const el = document.getElementById(id);
      el.focus();
      el.select();
      window.document.execCommand('copy');
    }
</script>
</body>
</html>
